  // if(todoList.children.length>10){ 
    //     alert('Enough for today');
    //     return; 
    // }


//  for (let task of existingTasks) {
//         if (task.textContent.toLowerCase() === taskText.toLowerCase()) {
//             alert('Duplicate task! This goal already exists.');
//             taskInput.value = '';
//             return; // Прерываем выполнение, li не создастся
//         }
//     }